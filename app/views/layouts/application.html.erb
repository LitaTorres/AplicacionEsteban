<!DOCTYPE html>
<html>
  <head>
    <title>AplicacionEsteban</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="stylesheet" href="	https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <div class="container-fluid">
        <a class="navbar-brand ms-5" href="#">
          <%= image_tag '1.png', style: "width: 100px; height: 100px;" %>
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav ms-auto">
            <li class="nav-item border border-secondary">
              <%= link_to "INICIO", homes_index_path, class: "nav-link text-white" %>
            </li>
            <% if user_signed_in? %>
              <li class="nav-item border border-secondary">
                <%= link_to current_user.email, edit_user_registration_path, class: "nav-link text-white" %>
              </li>
              <li class="nav-item border border-secondary">
                <%= image_tag current_user.photo, class: "nav-link", style: "max-width: 100px; max-height: 100px;" %>
              </li>
              <li class="nav-item border border-secondary">
                <%= link_to "PUBLICACIONES", publications_path, class: "nav-link text-white" %>
              </li>
              <li class="nav-item border border-secondary">
                <%= link_to "SALIR", destroy_user_session_path, method: :delete, class: "nav-link text-white" %>
              </li>
              <% if current_user.admin? %>
              <li class="nav-item border border-secondary">
                <%= link_to "REGISTRAR USUARIOS", new_user_registration_path, class: "nav-link text-white" %>
              </li> 
              <% end %>
            <% else %>
              <li class="nav-item border border-secondary">
                <%= link_to "INICIAR SESIÓN", new_user_session_path, class: "nav-link text-white" %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>




    <div class="container">
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>


    <%= yield %>
  </div>

<script src="	https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"> </script>
  </body>
</html>
