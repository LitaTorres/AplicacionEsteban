<%= form_with(model: postulation) do |form| %>
  <% if postulation.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(postulation.errors.count, "error") %> prohibited this postulation from being saved:</h2>

      <ul>
        <% postulation.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>

    <%= form.hidden_field :publication_id, value: current_user.id %>
  </div>

  <div>
    <%= form.label :publication_id, style: "display: block"  %>
    <%= form.select :publication_id, Publication.all.collect {|c| [c.title, c.id]} %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
